# FileHandle - RAII File Handler Library

## 📋 Описание проекта

FileHandle - это библиотека для работы с файловой системой на C++, реализующая парадигму RAII. Библиотека предоставляет безопасный и удобный способ работы с файлами, автоматически управляя их открытием и закрытием.

## Содержание
1. Основные возможности
2. Архитектура проекта
3. Быстрый старт
4. Использование библиотеки

## 🎯 Основные возможности
- RAII-обёртка для файлов - автоматическое управление ресурсами
- Безопасное открытие/закрытие файлов
- Чтение и запись по строкам (readLine/writeLine)
- Исключения для обработки ошибок
- Демонстрационная программа с примерами использования

## 🏗️ Архитектура проекта
```text
FILEHANDLEPROTEI/
├── CMakeLists.txt             # Основной файл сборки
├── FileHandler.h              # Заголовочный файл класса FileHandler
├── FileHandler.cpp            # Реализация методов FileHandler
├── functions.h                # Вспомогательные функции (если есть)
├── functions.cpp              # Реализация вспомогательных функций
├── main.cpp                   # Демонстрационная программа
├── tests/                     # Тестовые данные
│   └── test2.txt              # Тестовый файл для демонстрации
├── build/                     # Директория сборки (генерируется)
└── .vscode/                   # Конфигурация VS Code
```

## 🚀 Быстрый старт
Сборка проекта
```bash
# Клонирование репозитория (если применимо)
# git clone <repository-url>
# cd FILEHANDLEPROTEI

# Создание директории сборки
mkdir build && cd build

# Конфигурация проекта
cmake ..

# Сборка
make
```

Запуск программы
```bash
./FileHandlerProtei
```

💻 Использование библиотеки

```cpp
#include "FileHandler.h"

int main(){

    FileHandler f;
    char menu;
    std::string message;
    do{
        system("clear");
        std::cout << "1. FileHandler::readLine()" << std::endl;
        std::cout << "2. FileHandler::writeLine()" << std::endl;
        std::cout << "3. FileHandler::resetOffset()" << std::endl;
        std::cout << "4. Exit" << std::endl;
        try{
            std::cin >> message;
            if(message.size() > 1)
                throw std::runtime_error("Argument is too long");
            menu = message[0];
            if(menu < 49 || menu > 52)
                throw std::invalid_argument("Input is not valide number");
        }
        catch(const std::exception&e){
            std::cerr << e.what() << std::endl;
        }
        switch (menu)
        {
        case '1':{ std::cout << f.readLine() << std::endl; break; }
        case '2':{ 
            system("clear");
            std::cout << "Enter a message to write to file" << std::endl;
            cinFlush();
            std::getline(std::cin, message);
            f.writeLine(message);
            break;
        }
        case '3':{ f.resetOffset(); }
        }
        system("read -p 'Press Enter to continue...' var");
    } while(menu != '4');
    return 0;
}
```

Методы взаимодействия:
```cpp
// Последовательное чтение строк из файла
// Возвращает считанную строчку
std::string readLine();

// Запись строки в файл
void writeLine(std::string data);

// Сброс смещения на начало файла
void resetOffset();
```


